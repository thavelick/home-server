server {
	server_name {{wallabag_domain}};
	root {{wallabag_site_root}}/public_html/web;
	access_log {{wallabag_site_root}}/logs/access.log;
	error_log {{wallabag_site_root}}/logs/error.log;
	location / {
	    try_files $uri /app.php$is_args$args;
	}
	location ~ ^/app\.php(/|$) {
	    fastcgi_pass unix:/run/php/php7.4-fpm.sock;
	    fastcgi_split_path_info ^(.+\.php)(/.*)$;
	    include fastcgi_params;
	    fastcgi_param  SCRIPT_FILENAME  $realpath_root$fastcgi_script_name;
	    fastcgi_param DOCUMENT_ROOT $realpath_root;
	    internal;
	}
	listen 80;

    # listen 443 ssl;
    # ssl_certificate /var/www/certs/{wallabag_domain}}/fullchain.pem;
    # ssl_certificate_key /var/www/certs/{wallabag_domain}}/privkey.pem;
    # include /etc/letsencrypt/options-ssl-nginx.conf;
    # ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

}

# server {
#     if ($host = {wallabag_domain}}) {
#         return 301 https://$host$request_uri;
#     }
# 	server_name {wallabag_domain}};
#     listen 80;
#     return 404;
# }
